import React from 'react';
import { Container } from 'semantic-ui-react';

const content = <p>Here's how to use the bot</p>;

const Instructions = () => <Container content={content} text />;

export default Instructions;
